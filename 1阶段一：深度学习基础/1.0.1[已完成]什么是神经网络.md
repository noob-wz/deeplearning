相比于传统的机器学习，神经网络大大减少了对人工特征工程的依赖，通过训练自动提取特征。神经网络是受生物神经系统启发，一个复杂的神经网络中，有多个神经层，每层网络中又有多个神经元。每个神经元把上一层的输出作为输入特征，经过加权求和加上偏置，再经过激活函数非线性处理之后得到新的特征，该层神经网络中所有神经元的输出组合在一起，得到新的特征集合作为下一层神经网络的输入。到最后根据业务需要，在输出层进行分类或者回归处理。神经网络的本质是一个多层嵌套的非线性函数
