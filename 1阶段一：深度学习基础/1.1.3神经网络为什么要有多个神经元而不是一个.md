**神经网络中的每个神经元都是一次特征提取，神经元的个数越多，代表能够提取的复杂特征就越多，如果只有一个神经元，那么模型的表达能力就非常差。同传统机器学习一样，模型的表达能力差，那么就会对人工特征工程更加依赖，这样才能保证模型有不错的拟合复杂规律的能力，同时不可避免的人工特征工程会有“维度爆炸”和“模型状态对个别特征权重的变化极度敏感”的问题。**

所以，神经网络中一般都是要多个神经元而非一个。