8. **黑盒封装**：       
相比于我们纯手工定义 `w` 和 `b`，当你实例化一句 `self.fc = nn.Linear(10, 5)` 时，PyTorch 在底层自动替你完成了哪两件最核心的物理准备工作？

---

我的回答：
（1）自动初始化形状正确的 w 和 b        
（2）并打上 require_grad=True的标签。       
然后，只要把数据传入这层，就会自动计算 `x @ w + b`