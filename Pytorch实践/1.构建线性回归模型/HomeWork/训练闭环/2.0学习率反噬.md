**学习率反噬**：        
在线性回归中，如果损失值突然变成了 `NaN`（Not a Number）或 `inf`，基于更新公式 $w = w - lr \times w.grad$，你认为最有可能出问题的超参数是哪一个？为什么？

我的回答：**lr出问题，如果损失值变成了NaN，那么可能是学习率设置过大，导致一步踏出去太多，比如踏到Loss曲线对面上坡比现在更高的位置，那么参数更新就会如此反复震荡，而Loss的值不断变大最终超出了物理内存的范围，导致梯度爆炸**