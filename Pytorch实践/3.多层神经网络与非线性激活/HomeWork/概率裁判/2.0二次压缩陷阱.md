**二次压缩陷阱**：      
为什么绝对不能在模型的 `forward` 函数的最后加上 `nn.Softmax()`，然后再把输出传给 `nn.CrossEntropyLoss()`？

---

我的回答：**因为交叉熵损失函数已经包含了对原始分数的softmax处理，得到[0,1]之间的值。如果交叉熵函数之前已经处理了一遍，那么交叉熵中再经过softmax压缩之后得到的概率之间的区分度太小**

---

