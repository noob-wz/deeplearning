**神经元脑死亡**：      
什么是 Dead ReLU 现象？它是由于偏置 $b$ 变得过大还是过小造成的？在代码层面，它会导致反向传播时发生什么致命后果？

---

我的回答：**Dead ReLU现象是指神经网络中某个神经元的ReLu激活函数的梯度为0，无论优化器怎么更新，参数再也不会发生变化。这是由于偏置 $b$ 变的过大造成的。代码层面，会导致反向传播时梯度一直是0，不再更新**


