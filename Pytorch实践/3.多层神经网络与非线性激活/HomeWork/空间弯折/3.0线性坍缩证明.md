 **线性坍缩证明**： 
 假设有三层网络：`y1 = x @ w1`，`y2 = y1 @ w2`，`y3 = y2 @ w3`，且中间没有任何激活函数。请用一句话或简单的代数结合律证明，这三层网络完全可以用一层来替代。

---

我的回答：
 `y2 = y1 @ w2 = (x @ w1) @ w2`      
 `y3 = y2 @ w3 = [(x @ w1) @ w2] @ w3`
 所以，无论线性层有多少层，经过矩阵乘法之后可以用一个线性层来表示