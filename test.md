### 阶段 0：底层引擎与单细胞神经元（Stage 0）

**第一部分：张量与维度对齐的生死局**
1. **维度陷阱**：假设 `y_pred`（模型预测值）的形状是 `(32, 1)`，而你的 `y_true`（真实标签）由于某种原因形状是 `(32,)`。当你执行 `loss = ((y_pred - y_true)**2).mean()` 时，代码不会报错，但会发生极度诡异的物理现象。请利用“广播机制”推演一下，相减后产生的中间张量形状会变成什么？这会对损失值的计算造成什么毁灭性打击？
2. **矩阵乘法**：已知输入 `x` 形状是 `(64, 10)`，你要让它经过一个线性层得到 `(64, 5)` 的输出。请问底层权重矩阵 `w` 和偏置向量 `b` 的严格形状应该分别是什么？

**第二部分：求导引擎（Autograd）的底层逻辑**
3. **叶子节点断裂**：有新手写了这样的代码：`w = torch.tensor([1.0]); w = w * 2.0; w.requires_grad = True`。当执行反向传播后，`w.grad` 会有值吗？为什么？
4. **引擎悬挂**：在执行参数更新 `w -= lr * w.grad` 时，如果不加上 `with torch.no_grad():`，除了会导致计算图混乱，在内存/显存的物理层面上会发生什么具体的灾难？
5. **梯度累加设计**：为什么 PyTorch 在执行 `loss.backward()` 后，不默认自动把上一次的梯度清零，而是默认“累加”？这个反直觉的设计在显存极小的情况下能解决什么真实工程问题？

**第三部分：优化器与模型的解耦**
6. **优化器盲区**：你定义了两个网络 `model_A` 和 `model_B`。前向传播写的是 `y = model_A(x) + model_B(x)`。但你的优化器是这样初始化的：`optimizer = torch.optim.SGD(model_A.parameters(), lr=0.01)`。请推演：反向传播时，`model_B` 的参数有梯度吗？在 `.step()` 时，`model_B` 的参数会更新吗？
7. **暴力抹零**：为什么我们必须用 `optimizer.zero_grad()`（或者 `w.grad.zero_()`）来清空梯度，而绝对不能写成 `w.grad = 0`？
8. **黑盒封装**：相比于我们纯手工定义 `w` 和 `b`，当你实例化一句 `self.fc = nn.Linear(10, 5)` 时，PyTorch 在底层自动替你完成了哪两件最核心的物理准备工作？

**第四部分：训练闭环**
9. **闭环默写**：请不用代码，仅用自然语言，按绝对正确的先后顺序，写出 PyTorch 标准训练循环（for epoch...）里最核心的 5 个动作。
10. **学习率反噬**：在线性回归中，如果损失值突然变成了 `NaN`（Not a Number）或 `inf`，基于更新公式 $w = w - lr \times w.grad$，你认为最有可能出问题的超参数是哪一个？为什么？

---

### 阶段 1：多层视觉网络与数据管道（Stage 1）

**第五部分：数据管道（Data Pipeline）**
11. **__getitem__ 的契约**：在 `Dataset` 中，如果你的 `__getitem__` 返回了一张形状为 `(3, 224, 224)` 的图像特征 `x`，和一个形状为纯标量 `()` 的标签 `y`（例如数字 `7`）。当 `DataLoader` 设置 `batch_size=64` 时，最终吐出的 `y_batch` 的形状是什么？如果 `y` 返回的是 `(1,)`，最终 `y_batch` 形状又是什么？
12. **打乱的代价**：为什么在训练集（Train Set）上必须设置 `shuffle=True`，而在验证集/测试集（Val/Test Set）上绝对不能设置 `shuffle=True`？

**第六部分：空间弯折（MLP 与 ReLU）**
13. **展平操作**：`DataLoader` 吐出的图像批次形状是 `(128, 3, 28, 28)`。你要把它喂给输入特征数为 2352（即 $3 \times 28 \times 28$）的 `nn.Linear` 层。请写出紧贴着前向传播第一步的那行 `.view()` 展平代码。
14. **神经元脑死亡**：什么是 Dead ReLU 现象？它是由于偏置 $b$ 变得过大还是过小造成的？在代码层面，它会导致反向传播时发生什么致命后果？
15. **线性坍缩证明**：假设有三层网络：`y1 = x @ w1`，`y2 = y1 @ w2`，`y3 = y2 @ w3`，且中间没有任何激活函数。请用一句话或简单的代数结合律证明，这三层网络完全可以用一层来替代。

**第七部分：概率裁判（CrossEntropyLoss 与 Softmax）**
16. **Logits 的物理意义**：网络最后一层直接输出的数值（Logits）是合法的概率吗？合法的多分类概率必须满足哪两个死条件？
17. **二次压缩陷阱**：为什么绝对不能在模型的 `forward` 函数的最后加上 `nn.Softmax()`，然后再把输出传给 `nn.CrossEntropyLoss()`？
18. **数据类型铁律**：`nn.CrossEntropyLoss(logits, y_true)`。假设 `logits` 的形状是 `(32, 10)`，`y_true`（里面存的是类别的索引数字）的形状必须是什么？它的张量数据类型（dtype）必须是什么（比如 float 还是 long）？

**第八部分：部署推理（Inference）**
19. **手动解包概率**：模型训练完了。现在送入一张新图片，模型输出了形状为 `(1, 10)` 的 `output`。请写出精确的 PyTorch 代码，求出模型预测这张图为“类别 7”的百分比概率。
20. **双重保险**：在模型部署阶段，为什么我们既要调用 `model.eval()`，又要加上 `with torch.no_grad():`？它们在底层分别关闭了什么（虽然我们暂时还没讲 Dropout 和 BatchNorm，你可以基于你对 no_grad 的理解来回答后半部分）？

---

深呼吸，把你的答案抛过来吧。哪怕是错的，我也能根据你的错误推断出你的哪根神经搭错了线。等你交卷。