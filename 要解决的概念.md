矩阵乘法：内维匹配，外维保留（未解决）

对于输出是关于“是否”的分类问题，如果代价函数坚持使用MSE会有什么影响。即分类问题的概率要求输出为0到1之间，线性加权求和 + 激活函数后使用MSE有什么问题？为什么变成非凸函数，图像怎么看

笔记中代价函数的两个陷阱怎么理解，是否真正掌握？

逻辑回归的代价函数几何图是折纸形状的？为什么有一个批量的范围内，误差都是0；
为什么狭长的地形会走之字形

模型训练每个过程开始之前要经过怎样的思考：
（1）选择代价函数：要确定是回归问题还是分类问题，是否需要考虑异常点的影响
（2）选择优化算法：
    - 需要考虑数据量的大小，决定了是梯度下降还是正规方程；如果是梯度下降，是考虑批量梯度下降还是小批量梯度下降；如果是小批量下降，是考虑随机梯度下降还是 mini-batch 梯度下降；
    - 需要考虑特征尺度，决定了是否需要特征缩放；
    - 需要考虑是否需要早停机制，决定了是否需要监控验证集的误差；
    - 需要考虑使用什么样的学习率，是否需要学习率衰减
    - 需要考虑什么样的优化器（Adam还是普通梯度下降）
（3）选择模型架构：确定线性模型还是非线性模型；是否需要正则化；
等等

梯度下降天生的就有陷入局部最优解的困境；而且对于特征不归一化的情况，梯度下降会走之字形；对于学习率过大，可能会导致梯度爆炸，学习率过小，收敛过慢

梯度爆炸到底是什么

为什么最佳学习率不是Cost最低的那个点，而是曲线下降最陡峭的那个区间中间的值。

点积是向量和向量的乘积，还是向量和矩阵的乘积

Z-score到底是什么

记录每次更新后误差与历史最低误差之间的幅度大小，并且设置一个阈值，同时增加一个耐心值，耐心值可以自己设定大小，比如5。如果更新后的幅度小于设定的阈值，就把耐心值加一，并继续下一次更新；如果高于阈值，耐心值清零，继续更新。直到出现连续五次误差变化小于阈值，耐心值会变成5，此时终止训练。


如果多项式回归不能预测未来，那么有什么意义呢，那不就是过拟合数据吗，也没有实际意义

多项式回归特征缩放后缩放后的每个特征的权重影响是一样的，梯度下降时不同角度也都是圆滑的碗。那么为什么为了防止过拟合的时候，仍然会存在“幂次高的特征的权重影响大”的问题，特征缩放不是把问题解决了吗，换句话说，特征缩放不久等同于压成一次幂了吗

多项式回归特征缩放后，每个特征对模型的影响是一样的，是不是就是高次幂都被压成一次幂了，那为什么还说“是因为某些高次幂特征的权重 ww 变得极其巨大”，而Loss从狭长的峡谷变成平滑的“碗”了，怎么还要进行正则化处理呢。到底特征缩放但不正则化的拟合曲线是什么样的、Loss曲线是什么样的？特征缩放且正则化的拟合曲线是什么样的，Loss曲线是什么样的？这些概念之间到底是什么联系？请你写一个html动画演示说明，能可视化图表解释的就尽量少用文字说明