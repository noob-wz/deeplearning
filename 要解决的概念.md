笔记中代价函数的两个陷阱怎么理解，是否真正掌握？

逻辑回归的代价函数几何图是折纸形状的？为什么有一个批量的范围内，误差都是0；


记录每次更新后误差与历史最低误差之间的幅度大小，并且设置一个阈值，同时增加一个耐心值，耐心值可以自己设定大小，比如5。如果更新后的幅度小于设定的阈值，就把耐心值加一，并继续下一次更新；如果高于阈值，耐心值清零，继续更新。直到出现连续五次误差变化小于阈值，耐心值会变成5，此时终止训练。


如果多项式回归不能预测未来，那么有什么意义呢，那不就是过拟合数据吗，也没有实际意义

多项式回归特征缩放后缩放后的每个特征的权重影响是一样的，梯度下降时不同角度也都是圆滑的碗。那么为什么为了防止过拟合的时候，仍然会存在“幂次高的特征的权重影响大”的问题，特征缩放不是把问题解决了吗，换句话说，特征缩放不久等同于压成一次幂了吗

正则化 以及 **，加入正则化，相当于主动放大了模型的偏差，正则化之后得到的最优模型不会偏差过大(欠拟合)呢？**，这是需要考虑的问题

既然神经网络中只要有足够多的神经元，组合在一起就可以拟合任意的复杂曲线，那么只要设立一个隐藏层就可以了，为什么还要设置多个隐藏层呢，在面对复杂问题的时候？

Part A week3 day4 模型欠拟合问题分析




